untuk memulai app, perlu 3 terminal:
1. untuk gradlew build:
  - gradlew build --continuous
3. untuk gradlew bootRun:
  - gradlew bootRun
4. npm start:
  - cd frontend
  - npm start

dashboard dijalankan pada link http://localhost:3000
